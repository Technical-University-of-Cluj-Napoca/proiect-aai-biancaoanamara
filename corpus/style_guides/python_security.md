# Python Secure Coding Guidelines

1. Never use `eval()` on untrusted input.
2. Use parameterized queries for database access to prevent SQL Injection.
3. Keep your dependencies updated.
4. Hash passwords using strong algorithms like Argon2 or bcrypt.
